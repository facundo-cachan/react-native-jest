import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  base: {
    fontFamily: 'Arial'
  },
  pressable: {
    backgroundColor: 'red',
    borderColor: 'green',
    borderWidth: 1
  }
});

export default styles;
